{
  "name": "vibetunnel-pty",
  "version": "0.1.0",
  "description": "Native PTY implementation for VibeTunnel",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "vibetunnel-pty",
    "triples": {}
  },
  "scripts": {
    "build": "cd crates/napi-addon && napi build --platform --release ../..",
    "build:debug": "cd crates/napi-addon && napi build --platform ../..",
    "test": "node test/napi-addon.test.js",
    "test:rust": "cargo test --all"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  },
  "engines": {
    "node": ">= 10"
  }
}